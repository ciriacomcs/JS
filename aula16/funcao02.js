function soma(n1=0, n2=0) {   // (n1=0, n2=0) -- aqui definimos os parâmetros para n1 e n2, caso ele não seja definido na chamada da função
    return n1 + n2
}

// let res = soma(3, 4)  ------> pode ser usado dessa forma ou a forma escrita na linha abaixo
console.log(soma(3, 4))